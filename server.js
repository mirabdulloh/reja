console.log("it is working");
const express = require("express");
const http = require("http")
const app = express();
const fs = require("fs")



let user;
fs.readFile("database/user.json", "utf8", (err,data)=>{
    if(err){
        console.log("ERROR",err);
    }else{
        user = JSON.parse(data)
    }
});

// 1 - Kirish kodlari
app.use(express.static("public"));
app.use(express.json());
app.use(express.urlencoded({extended:true}));

// 2 - sessions ga bogliq qodlar


// 3 -- views ga bogliq kodlar

app.set("views", "views");
app.set("view engine", "ejs");

// 4 -- Routing ga bogliq qodlar

// app.get("/hello", function(req, res){
//     res.end(`<h1 style="background:red">Hello world</h1>`)
// });
// app.get("/gift", function(req, res){
//     res.end(`<h1 style="background:red">You are in gift page</h1>`)
// });

app.post("/create-item",(req,res)=>{
    console.log(req.body);
    res.json({test:"success"});
});
app.get("/", function(req, res){
    res.render("harid");
});
app.get("/author",(req, res)=>{
    res.render("author",{user: user})
})

const server = http.createServer(app);
let PORT = 3000;
server.listen(PORT, function(){
    console.log(`server is working in this port: ${PORT}`);
});









































































































































































































































































































































































































































































































































































































































































































































































































































































